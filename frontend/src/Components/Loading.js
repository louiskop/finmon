import ReactLoading from "react-loading";

function Loading() {
  return (
    <div>
      <ReactLoading type="bars" height={300} width={150} color="#E4005A" />
    </div>
  );
}

export default Loading;
